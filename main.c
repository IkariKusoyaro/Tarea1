#include <stdio.h>
#include <stdlib.h>
#include "Librerias/Funciones.h"
#include "Librerias/Interfaz.h"
#include "Librerias/TDA/list.h"


int main(){
    short opcion;
    do{
       menu();
       printf("\nElija una opcion:");
       scanf("%hi", &opcion);
       if(opcion==0)break;

       switch(opcion){
           


       }
    }while(opcion!=0);
}